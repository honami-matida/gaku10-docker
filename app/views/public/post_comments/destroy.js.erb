$('#comment_index').html("<%= j(render 'public/post_comments/index',  post: @post, post_comments: @post_comments) %>");

$("#comment_flash_area").html("<div class='flash-message notice'>コメントを削除しました</div>");

setTimeout(function() {
  $("#comment_flash_area .flash-message").fadeOut(800, function() {
    $(this).remove();
  });
}, 2000);
